priority: 999 # Run after DNS forwarding is configured
help: Add and delete dnsmasq blacklisting, redirect IP, exclusions and URL sources

end:
    sudo _OFR_CONFIGURE=ok /config/scripts/update-dnsmasq -v

 